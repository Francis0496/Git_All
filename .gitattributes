# Normalize line endings: store LF in the repo, check out native on each OS.
* text=auto eol=lf

# Treat binary assets as binary (never touch line endings).
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.gif binary
*.ico binary
*.pdf binary
